console.log('JS is connected to the page!')

const spinner = document.getElementById('spinner');

const number_of_segments = 4;
const colors = ['yellow', 'black', 'green', 'teal']
const create_segments = (spinner, number_of_segments) => {
    const increment = 360 / number_of_segments;
    
}

create_segments(spinner,  number_of_segments)